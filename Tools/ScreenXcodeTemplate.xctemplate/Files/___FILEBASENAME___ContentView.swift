//  Copyright © ___YEAR___ ___ORGANIZATIONNAME___. All rights reserved.
//  Generated using Xcode Screen Template

import UIKit
import AppCoreUI

extension ___VARIABLE_productName:identifier___Context {
    
    final class ContentView: BaseView {
        
        // TODO: Implement Views
        // Use core ui for getting core components, strings and styling
        
        // EXAMPLE
        /*
        private(set) var floatingPanel: FloatingPanelController = {
            let floatingPanel = FloatingPanelController()
            floatingPanel.layout = MapIntrinsicLayout()
            floatingPanel.backdropView.dismissalTapGestureRecognizer.isEnabled = true
            return floatingPanel
        }()

        private lazy var actionsStackView: UIStackView = {
            let stackView = UIStackView()
            stackView.axis = .vertical
            stackView.alignment = .fill
            stackView.distribution = .equalSpacing
            stackView.spacing = spacing.vPadding
            return stackView
        }()

        private(set) lazy var mySettingActionButton = components.filledActionCaptionButton
        */
        
        override func setLayout() {
            // TODO: set up layout using snapkit
            /*
            backgroundColor = colors.backgroundPrimary
            
            addSubview(editProfileActionButton)
            addSubview(topSpaceView)
            addSubview(profileStackView)
            addSubview(middleSpaceView)
            addSubview(actionsStackView)
            addSubview(bottomSpaceView)
            
            setLayoutForEditProfile()
            setLayoutForProfileSection()
            setLayoutForBottomActions()
            */
        }
    }
}
